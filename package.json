{
  "name": "desktop-pets",
  "version": "0.1.0",
  "private": true,
  "description": "A desktop companion that decides whether to interrupt you \u2014 and that you can answer back.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "pnpm run build && vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "start": "pnpm run build && pnpm --filter @desktop-pets/host exec electron .",
    "smoke": "pnpm run build && pnpm --filter @desktop-pets/host exec electron . --smoke",
    "gen:pets": "pnpm run build && node packages/create-pet/dist/gen-bundled.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.30.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.35.0",
    "vitest": "^3.2.0"
  }
}
